config:
  activate:
    on-profile: default, local, development
tenants:
  tenant-b:
    issuer: ${auth.serverUrl}/realms/tenant-b
---

config:
  activate:
    on-profile: production
tenants:
  tenant-b:
    issuer: ${auth.serverUrl}/realms/tenant-b
